# divinecore-v2 — Code-First Runtime

The Python code-first runtime that runs in parallel to the n8n orchestration stack. Long-term, this replaces n8n for agent execution. Today, it's a foundation: API + worker + scheduler + broker, all containerised.

## Status

- **Foundation shipped** (commit `3c03553`)
- **CI/CD live** — pushes to `main` build api+worker images to GHCR and notify `#deploys` (commit `3573662`)
- **VPS deploy live (manual)** — stack is running on Hostinger VPS at `/root/divinecore-v2/` as of 2026-05-03. End-to-end echo task verified through Redis → worker → result. API bound to `127.0.0.1:8000` (localhost only, not internet-accessible yet).
- **Auto-deploy from CI pending** — deploy is currently triggered manually via `ssh root@srv1445995.hstgr.cloud "cd /root/divinecore-v2 && docker compose -f docker-compose.prod.yml pull && docker compose -f docker-compose.prod.yml up -d"`. Wiring CI to do this on every push to main is the next infra task.

## Structure

```
divinecore-v2/
├── api/
│   ├── main.py                ← FastAPI app
│   ├── settings.py            ← Pydantic Settings (REDIS_URL)
│   ├── requirements.txt
│   └── Dockerfile
├── worker/
│   ├── celery_app.py          ← Celery config + beat_schedule + task includes
│   ├── tasks.py               ← echo, heartbeat (demo)
│   ├── settings.py            ← Pydantic Settings (Airtable, OpenRouter, Fathom, Redis)
│   ├── team.py                ← TEAM_MEMBERS dict + email/name lookup
│   ├── integrations/
│   │   └── fathom/            ← Meeting recorder integration (see its .overview.md)
│   ├── requirements.txt
│   └── Dockerfile
├── docker-compose.yml         ← local dev (build:, volume mounts, uvicorn --reload)
├── docker-compose.prod.yml    ← what's deployed on VPS (image: from GHCR, env_file: .env)
└── .dockerignore
```

## Services (docker-compose.yml)

| Service | Image / Build | Notes |
|---------|---------------|-------|
| `redis` | `redis:7-alpine`, port 6379 | Broker AND result backend for Celery |
| `api` | `./api` | FastAPI on `:8000`. Mounts `./api` for live reload in dev. |
| `worker` | `./worker` | Celery worker. Same image as `beat`. |
| `beat` | `./worker` (override CMD) | Celery Beat scheduler. Runs `tasks.heartbeat` every 30s and `tasks.poll_fathom_recordings` every 10 min. |

`REDIS_URL=redis://redis:6379/0` is passed to all three Python services.

## Endpoints (today)

- `GET /` — health
- `POST /tasks/echo` — submit a Celery task, returns `task_id`
- `GET /tasks/{task_id}` — poll status + result

## Scheduled tasks

- `tasks.heartbeat` — every 30s (liveness)
- `tasks.poll_fathom_recordings` — every 10 min (Fathom integration; see below)

## Integrations

- **Fathom** — beat polls Fathom's REST API every 10 min for new meetings, writes them to Airtable's `Meetings` table, and creates Pulse Task rows for action items assigned to opted-in team members. No public ingress required — fully outbound. See [worker/integrations/fathom/.overview.md](worker/integrations/fathom/.overview.md).

## Run locally

```bash
cd divinecore-v2
docker compose up --build
```

## Deploy to VPS (manual, until CI is wired)

The VPS already has `/root/divinecore-v2/docker-compose.prod.yml` and is logged into GHCR (PAT cached in `/root/.docker/config.json`). Update by:

```bash
ssh root@srv1445995.hstgr.cloud \
  "cd /root/divinecore-v2 && \
   docker compose -f docker-compose.prod.yml pull && \
   docker compose -f docker-compose.prod.yml up -d"
```

The compose file in `/root/divinecore-v2/` should mirror `divinecore-v2/docker-compose.prod.yml` in this repo. If you change one, sync the other.

## What goes here

- New API routes → add directly in `api/main.py` (or extract to `api/routes/<name>.py` if it grows)
- New API env vars → `api/settings.py`
- New worker integrations → `worker/integrations/<service>/`, add to `include` list in `celery_app.py`
- New worker env vars → `worker/settings.py`
- New scheduled jobs → add to `app.conf.beat_schedule` in `worker/celery_app.py`
- New service (e.g. a Discord listener) → new top-level folder + add to `docker-compose.yml`

## What does NOT go here

- n8n workflow JSONs — those go in the relevant module's `workflows/` folder.
- Agent system prompts — those go in the relevant module's `agents/` folder.
- Knowledge-base sync scripts — those go in the relevant module's `knowledge-base/` folder.
