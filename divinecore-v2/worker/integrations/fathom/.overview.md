# worker/integrations/fathom/

Pulls meeting recordings from Fathom's REST API on a schedule, categorizes them, and writes them to Airtable. Fully outbound — no public ingress required.

## Flow

```
Beat (every 10 min)
  -> tasks.poll_fathom_recordings              # poller.py
       -> read last-polled checkpoint from Redis (key: fathom:last_polled_at)
       -> GET https://api.fathom.ai/external/v1/meetings
            ?created_after=<checkpoint>
            &include_summary=true
            &include_transcript=true
            &include_action_items=true
            &cursor=...                        # paginates until exhausted
       -> for each new meeting:
            tasks.process_fathom_recording.delay(item)   # processor.py
              -> categorizer.classify(item)              # heuristic + OpenRouter fallback
              -> airtable_writer.upsert(item, category)  # idempotent on Meeting ID
              -> pulse_bridge.create_tasks_for_opted_in(item, meeting_record)
       -> save new checkpoint timestamp to Redis
```

## Files

| File | Role |
|------|------|
| `poller.py` | Celery beat task. Pulls new meetings since last checkpoint, fans out one `process_fathom_recording` task per meeting, advances the checkpoint. |
| `processor.py` | Per-meeting Celery task. Composes categorize -> upsert -> maybe-pulse-tasks. |
| `categorizer.py` | Two-stage classifier: heuristic on attendee composition, then OpenRouter (default `openai/gpt-4o-mini`) fallback. |
| `airtable_writer.py` | Upserts the `Meetings` row. Looks up by `Meeting ID` first; updates if found, creates if not. Truncates transcripts >90k chars. |
| `pulse_bridge.py` | Creates rows in Pulse's existing `Task` table for action items whose assignee is a team member with `auto_pulse_tasks: True`. Otherwise no-op. |

## Airtable schema (Meetings table, DivineCore base)

`Meeting ID` (primary), `Title`, `Date`, `Duration (min)`, `Owner`, `Attendees`, `External Attendees`, `Category` (single-select: client/strategy/cofounder/sales/other), `Summary`, `Transcript`, `Transcript URL`, `Recording URL`, `Processed At`.

Action items are NOT stored on the Meetings row. They become rows in Pulse's `Task` table for opted-in users; otherwise Fathom remains the source of truth (visible via `Recording URL`).

## Fathom API details

- Base URL: `https://api.fathom.ai/external/v1`
- Auth header: `X-Api-Key: <key>`
- Endpoint used: `GET /meetings` with `created_after`, `cursor`, `include_summary=true`, `include_transcript=true`, `include_action_items=true`
- Response shape: `{ limit, next_cursor, items: [...] }`. Each item carries `recording_id`, `meeting_title`, `recording_start_time`/`recording_end_time`, `recorded_by`, `calendar_invitees`, `default_summary`, `transcript`, `action_items`, `url`, `share_url`.

## Env vars

Read from `worker/settings.py` (Pydantic Settings):
- `FATHOM_API_KEY`, `FATHOM_API_BASE_URL` (default `https://api.fathom.ai/external/v1`), `FATHOM_POLL_LOOKBACK_HOURS` (default 24 — only matters on cold start when the Redis checkpoint is empty)
- `AIRTABLE_API_KEY`, `AIRTABLE_BASE_ID`
- `AIRTABLE_MEETINGS_TABLE` (default `Meetings`), `AIRTABLE_TASKS_TABLE` (default `Task`)
- `OPENROUTER_API_KEY`, `OPENROUTER_BASE_URL` (default `https://openrouter.ai/api/v1`), `CATEGORIZER_MODEL` (default `openai/gpt-4o-mini` — same model Pulse uses)

## Opt-in config

Lives in [worker/team.py](../../team.py):

```python
TEAM_MEMBERS = {
    "yhpunk@oneboxagency.com": {"name": "yhpunk", "auto_pulse_tasks": True},
}
```

Add Mayank (and others) when their work emails are confirmed. Flip `auto_pulse_tasks` to opt them into auto Pulse Task creation from action items.

## Polling cadence + checkpoint

Beat schedule key: `poll-fathom-every-10m` (in `worker/celery_app.py`). Checkpoint timestamp persists in Redis at `fathom:last_polled_at`. On cold start (no checkpoint), poller looks back `FATHOM_POLL_LOOKBACK_HOURS` (default 24).

To force a re-pull of older meetings:
```bash
docker compose exec redis redis-cli DEL fathom:last_polled_at
# then either wait 10 min for next beat cycle, or:
docker compose exec worker celery -A celery_app call tasks.poll_fathom_recordings
```

## Categorization

Heuristic order:
1. No external attendees + 2 internal -> `cofounder`
2. No external attendees + 3+ internal -> `strategy`
3. Any external attendee on `CLIENT_DOMAINS` -> `client`
4. Any external attendee on `SALES_PROSPECT_EMAILS` -> `sales`
5. Otherwise -> OpenRouter LLM fallback (returns one of the five categories or `other`)

Client/sales lists are constants in `categorizer.py`. Promote to an Airtable lookup table only when they grow past ~10 entries each.

## Open questions

1. Fathom plan tier (Team plan recommended for clean attribution).
2. Mayank's work email -> add to `TEAM_MEMBERS`.
3. Field-name assumptions on `calendar_invitees[].email` and `action_items[].assignee/.description` — verified against Fathom docs at integration-time, but the first real poll will confirm.
