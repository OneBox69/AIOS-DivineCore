# Pulse — Module 4 (Alert + Awareness)

The only module currently **active in production**. Lives across all DivineCore Discord channels, runs morning digests, sends progressive deadline reminders, and escalates breaches.

## Status: ACTIVE

The live execution lives in n8n on the VPS. The local folder here is mostly scaffold — production reality is in n8n workflows + Airtable.

## Live production references

| Component | Where it lives | Notes |
|-----------|----------------|-------|
| Bot | Discord — name "Pulse (APP)" | Addresses Mayank as "Sir" |
| Model | GPT-4o mini via OpenRouter | Cheap inference for high-frequency reminders |
| Memory | Postgres Chat Memory | session key = username |
| Task DB | Airtable — base "DivineCore", table "Task" | Fields in CLAUDE.md §6 Module 4 |
| Pulse Agent workflow | n8n | https://n8n.srv1445995.hstgr.cloud/workflow/veDyMBIC6uCoULYb |
| Reminder Sequence workflow | n8n | https://n8n.srv1445995.hstgr.cloud/workflow/lpkxm5wGHXJF53ru |
| Server message webhook | n8n | `16202f28-06f5-4d4a-8060-97dcd9986c3b` |
| DM webhook | n8n | `d0ecdaad-f267-4fd5-8719-36ffe007067f` |

## Functions (live today)

- **Morning digest** at 03:30 IST — active tasks per channel
- **Progressive reminders** at 1/3 and 2/3 of deadline window
- **Breach detection** — deadline passed + status ≠ done → immediate alert
- **Admin commands** via `#pulse` (private channel only)
- Responds only when tagged in public channels

## Folder structure

```
pulse/
├── agents/      ← Pulse system prompt (scaffold — live prompt in n8n node)
└── workflows/   ← n8n workflow JSON exports (scaffold — needs initial export)
```

## Critical next step

**Export the live n8n workflows to JSON and commit them to `pulse/workflows/`.** Right now production state is only in n8n — no version control. Per CLAUDE.md §12: "Commit workflow exports."

## What goes here

- `agents/pulse.md` — system prompt (mirror of the one in n8n) for review/iteration
- `workflows/pulse-agent.json` and `workflows/reminder-sequence.json` — exports of the live n8n workflows
- Future: any code that augments Pulse (e.g. a v2 Python task that handles digest generation if/when we move it off n8n)

## What does NOT go here

- The actual bot infrastructure (Discord middleware, n8n hosting) — those live in `infrastructure/` and on the VPS.
- Task data — that's in Airtable, not files.
