# shared/context — Cross-Module Identity Context

The single source of truth for **who DivineSide is, who Mayank is, how we sound, who we're talking to, and where we're heading.** Any agent that produces text aimed at humans (sales emails, YouTube scripts, LinkedIn posts, DMs, follow-ups) MUST load the relevant files here before generating output.

## Why this folder exists

CLAUDE.md is for agents understanding the **system architecture** — modules, infrastructure, conventions. It is not voice-shaped. An agent reading CLAUDE.md to write a LinkedIn post would produce something that sounds like documentation.

`shared/context/` is the **voice-shaped** layer. Same facts, different framing — written so an LLM can lift identity, tone, positioning, and target audience directly into copy.

This is a deliberate split:
- **CLAUDE.md** — what the system IS (for agents that build/maintain the system)
- **shared/context/** — who the brand IS (for agents that speak as the brand)

## Files

| File | What it contains | Sourced from |
|------|------------------|--------------|
| `business-info.md` | DivineSide identity: mission, model, stage, philosophy, what we sell vs. don't | CLAUDE.md §1–§3, plus voice-shaped framing |
| `voice.md` | Brand-level tone — descriptors, do/don't rules, banned phrases, per-channel formatting | Mayank-supplied. Template for now. |
| `strategy.md` | Current positioning, distribution channels, content cadence, monetisation layers | CLAUDE.md §8–§9 |
| `audience.md` | Who we write FOR — personas, pain points, the kind of person who'd buy / subscribe / share | Derived from §9 positioning |
| `mayank.md` | **Mayank's persona & voice.** Channel-agnostic. Role, worldview, POV, signature lines. Load for any Mayank-voiced content. | CLAUDE.md §7 + things only Mayank can write |
| `pang.md` | **Pang's persona & voice.** Channel-agnostic. Personality (5 expanded traits), writing style, content pillars, hot takes, what he never does, constant reinforcement. Load for any Pang-voiced content. | Adapted from Pang's prior Content Autopilot persona doc |
| `linkedin-playbook.md` | **Pang's LinkedIn-specific tactical playbook.** Hooks/CTAs/closings swipe files, 3 post frameworks (PAS/SLA/Case Study), writing system flow, growth strategy, profile copy. References `pang.md` for persona. | Adapted from Pang's prior Content Autopilot playbook |
| `sales-playbook.md` | **End-to-end sales SOP** — qualification, pre-call audit checklist, locked pricing (£5k pilot / £2k Foundation retainer), 24h audit doc template, follow-up cadence, pilot→retainer transition. References `sales-discovery-call.md` for the live-call playbook. The training doc the future Sales OS will load from. | Conversation 2026-05-04 |
| `sales-discovery-call.md` | **45-min discovery call playbook.** Call structure, scripts, decision-authority branches (A/B/C), why-triad diagnosis, pipeline mapping, numbers-anchor technique, offer framing with temperature checks, risk-reversal language, common-objection responses. Loaded before any live discovery call. | Conversation 2026-05-04, integrating Mayank's prior call notes |
| `offer.md` | **The locked offer.** Headline promise (*"Your business, running on AI."*), naming discipline (no "AI OS" externally), pilot phases, pricing (£2.5k founding / £5k standard / £2k retainer), the universal hours-saved guarantee (8+ hrs/week back by week 4), scope envelope, expansion path. Source of truth for what we sell. | Conversation 2026-05-04 |
| `delivery.md` | **The 6-week pilot delivery SOP.** Two parallel tracks (AI brain + workflow). Phase 0 pre-onboarding → Phase 1 context (kickoff + validation calls) → Phase 2 data + baseline → Phase 3 workflow build + ship → Phase 4 handoff. The 8 brain-category files (`identity`/`strategy`/`products`/`customer`/`operations`/`channels`/`numbers`/`decisions` — last one is the moat). Tech access protocol (Bitwarden). Brain maintenance during weekly retainer check-in. | Conversation 2026-05-04 |
| `workflow-build.md` | **Workflow build SOP** — Phase 3 of delivery in detail. Prereq check (APIs + brain + scope), where the workflow lives (n8n for ≤20-node SaaS glue, divinecore-v2 for LLM/agentic), 6-step build flow, where things commit. Starter doc — grows with each pilot. | Conversation 2026-05-04 |
| `intake-form.md` | **Client intake form** — 10-question Typeform sent in Phase 0 (paste-ready content + question types + free-tier discipline). Captures easy facts async; voice/strategy/decisions stay in the kickoff call. | Conversation 2026-05-04 |

## How agents should use this

**Email copy agent (Sales OS — outbound):**
1. Load `business-info.md` (what we sell), `voice.md` (tone), `audience.md` (who they are)
2. Plus `sales-playbook.md` §01–§02 (target profile + qualifier line for the cold-email reply flow)
3. Plus the persona file for whoever the email is voiced as (`mayank.md` / `pang.md`)
4. Plus: `sales-os/knowledge-base/` (Hormozi frameworks)
5. Compose

**Sales discovery / proposal agent (Sales OS — inbound from a booked call):**
1. Load `offer.md` (what we sell — locked headline, pricing, guarantee, scope envelope)
2. Plus `delivery.md` (what we ship — so the call doesn't promise things delivery can't do)
3. Plus `sales-playbook.md` (broader process — qualification, pre-call audit, 24h audit doc template, follow-up cadence)
4. Plus `sales-discovery-call.md` (the live-call playbook — structure, scripts, branches, objection handling)
5. Plus `business-info.md` + `audience.md` (so it understands who's on the other side of the call)
6. Plus the persona file for whoever's running the call (`mayank.md` / `pang.md`)
7. Plus `voice.md` for any written output (proposal, follow-ups)
8. Run pre-call audit / generate audit doc / draft follow-ups

**Pilot delivery agent (inbound from a signed pilot):**
1. Load `delivery.md` (the SOP — phases, tracks, brain categories, tech access)
2. Plus `offer.md` (so it knows what we promised them)
3. Plus `intake-form.md` (the Phase 0 form content)
4. Plus the persona file for whoever owns the client relationship (`mayank.md` / `pang.md`)
5. Plus `voice.md` for any client-facing docs (intake welcome copy, handoff doc, kickoff agenda)
6. Run kickoff / brain synthesis / validation / weekly retainer check-ins

**Workflow build agent (Phase 3 of delivery):**
1. Load `workflow-build.md` (build SOP)
2. Plus `delivery.md` (so it understands where this fits in the pilot)
3. Plus `offer.md` (so it knows the scope envelope and the hours-saved guarantee)
4. Plus the client's brain repo (`<brand>-ai-brain` on GitHub)
5. Build → test → ship → measure

**LinkedIn content agent — Pang (first-person, e-comm beauty ICP):**
1. Load `pang.md` (persona — who is writing)
2. Plus `linkedin-playbook.md` (LinkedIn tactics — hooks, frameworks, profile)
3. Plus `voice.md` (brand-level banned phrases, general formatting) + `audience.md` (broader DivineSide reader for cross-channel context)
4. Plus `branding-os/knowledge-base/` (Kallaway frameworks) + working examples (Supabase)
5. Compose

**YouTube / video content agent — Mayank (Branding OS, IMAGYN/LYRA):**
1. Load `mayank.md` (persona — Mayank's POV)
2. Plus `voice.md`, `strategy.md` (positioning angle)
3. Plus `branding-os/knowledge-base/` (Kallaway frameworks) + working examples (Supabase)
4. Compose

**Pattern**: persona file (`pang.md` / `mayank.md`) is always loaded for any first-person content. Channel-specific tactical playbooks (like `linkedin-playbook.md`) layer on top when the channel demands platform-specific structure.

**Outreach DM agent (Networking OS):**
1. Load `business-info.md` (what we offer), `voice.md` (brand tone), `audience.md` (segment)
2. Plus: platform-specific conventions
3. Compose

## Loading discipline

- **Don't load every file every time.** Pick what's relevant for the task. A YouTube hook agent doesn't need `audience.md`'s full persona breakdown — only `voice.md` and the relevant persona file (`mayank.md` or `pang.md`).
- **Cite the source in the agent's system prompt.** Be explicit: "Use the tone described in `shared/context/voice.md`." This makes drift visible later.
- **When this content drifts from CLAUDE.md, update both.** They share facts; they shouldn't disagree.

## What does NOT go here

- **Architecture, infrastructure, module specs** — those stay in CLAUDE.md.
- **Knowledge bases / domain frameworks** — Kallaway lives in `branding-os/knowledge-base/` (Airtable-mirrored), Hormozi lives in `sales-os/knowledge-base/`. These are *expertise*, not *identity*.
- **Working examples / past content** — those live in Supabase + module folders. This is the identity layer, not the example library.
- **Personal data unrelated to brand voice** — health, finances, life logistics. Keep this folder narrow.
- **Secrets** — never. Reference only.

## Maintenance

- These files should evolve as the brand evolves. Treat them like a living style guide.
- When Mayank writes a piece of content that *feels* exactly right, mine it for `voice.md` updates.
- When a campaign launches or a positioning shifts, update `strategy.md` in the same week — agents will keep writing yesterday's pitch otherwise.
